import React from "react";
import "./footer.css";

const FooterBar = ({errorMsg, signMsg}) => {
    
    return (
        <footer className="App-footer">
      </footer>
    )
};

export default FooterBar;